services:
  auth-service:
    build:
      context: .
    container_name: auth-service
    ports:
      - "8003:8000"
    volumes:
      - .:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
